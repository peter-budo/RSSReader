apply plugin: 'android'
project.archivesBaseName = 'com.petermiklosko.rssreader'

dependencies {
    compile project(':core')
    compile 'com.android.support:support-v4:13.0.0'
}

android {
    compileSdkVersion 18
    buildToolsVersion '18.1.0'

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 16
    }
}
